#include <stdio.h>

int main() {
    int n, i, num, sum = 0;
    float mean;

    scanf("%d", &n);

    for(i = 0; i < n; i++) {
        scanf("%d", &num);
        sum += num;
    }

    mean = (float)sum / n;

    printf("Array Mean Value is %.2f\n", mean);

    return 0;
}
