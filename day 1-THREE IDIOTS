#include <stdio.h>

int main() {
    float x1, y1, x2, y2;
    
    
    scanf("%f %f %f %f", &x1, &y1, &x2, &y2);
    
    
    float mid_x = (x1 + x2) / 2;
    float mid_y = (y1 + y2) / 2;

    
    printf("Binoy's house is located at (%.1f,%.1f)\n", mid_x, mid_y);

    return 0;
}
