#include <stdio.h>

int main() {
    int n, i;
    long long product = 1;

    scanf("%d", &n);

    for(i = 1; i <= n; i++) {
        product *= i;
    }

    printf("Product of value is %lld\n", product);
    return 0;
}
